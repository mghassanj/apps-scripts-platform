[phases.setup]
nixPkgs = ['nodejs_22', 'npm']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'prisma migrate deploy && npm run start'

[variables]
FORCE_REBUILD = '2026-01-30-v4'
